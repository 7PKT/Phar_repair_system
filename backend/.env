# .env (CMU Remote Database Configuration)

NODE_ENV=development
PORT=5000

# Database Mode Selection
DATABASE_MODE=remote

# === REMOTE Database (CMU Production) ===
REMOTE_DB_HOST=ma.pharmacy.cmu.ac.th
REMOTE_DB_USER=pharmacyma
REMOTE_DB_PASSWORD=ma135!RX*jodn#7
REMOTE_DB_NAME=database_app_ma
REMOTE_DB_PORT=3306

# === LOCAL Database (Backup - ไม่ใช้ตอนนี้) ===
LOCAL_DB_HOST=localhost
LOCAL_DB_USER=root
LOCAL_DB_PASSWORD=
LOCAL_DB_NAME=repair_system
LOCAL_DB_PORT=3306

# JWT
JWT_SECRET=your_jwt_secret_key